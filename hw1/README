How to run test:

1. download the model:
	./download_model.sh
	After downloading, the model will be saved in ./model

2. run test:
	./hw1_rnn.sh $1 $2
	./hw1_cnn.sh $1 $2
	./hw1_best.sh $1 $2
	$1: the data directory, $2: output filename

Note: If you cannot run any shell script, remeber to chmod 755